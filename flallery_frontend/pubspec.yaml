name: flallery_frontend
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Dependencia para manejar solicitudes HTTP
  http: ^0.13.4

  # Dependencia para manejar el estado de la aplicación con arquitectura BLoC
  flutter_bloc: ^8.0.1

  equatable: ^2.0.3
  # Dependencia para manejar rutas de navegación
  # flutter_modular: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true